# --- Free text features: context detect + suggest-prompt ---
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- Free generative image editing (prompt -> transformed photo) ---
# Free token, NO credit card: https://huggingface.co/settings/tokens
# Leave blank to disable AI transforms (Looks + auto-enhance still work).
HF_TOKEN=
# HF_MODEL=black-forest-labs/FLUX.1-Kontext-dev

PORT=8787
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# GEMINI_TEXT_MODEL=gemini-2.5-flash
